import React, { Fragment, ReactElement } from 'react';

const SecurityPage: React.FC = (): ReactElement => {
  return (
    <Fragment>
      <h1>Security</h1>
    </Fragment>
  );
};

export default SecurityPage;
